class MyPriorityQueue {
    // push x to the queue, O(log(n))
    void push(int x) {
    }

    // pop the element with the highest priority, O(log(n))
    int pop() {
        return 0;
    }

    // get the element with the highest priority
    int peek() {
        return 0;
    }

    // get the number of elements in the queue
    int size() {
        return 0;
    }

}
